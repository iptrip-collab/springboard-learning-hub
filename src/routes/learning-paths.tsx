import { createFileRoute } from "@tanstack/react-router";
import { ArrowRight, Compass, Leaf, Sprout } from "lucide-react";
import { Button } from "@/components/ui/button";
import { IconBullet, PageHero, PageShell, SectionLabel } from "@/components/vasant-site";

export const Route = createFileRoute("/learning-paths")({ head: () => ({ meta: [{ title: "Learning Paths | Vasant" }, { name: "description", content: "Follow a focused learning path from first principle to a finished, useful outcome." }, { property: "og:title", content: "Learning Paths | Vasant" }, { property: "og:description", content: "Follow a focused learning path from first principle to a finished outcome." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: PathsPage });

function PathsPage() { const paths = [["The Calm Practitioner", "Build a steady practice for better work and clearer decisions.", "4 plots", Leaf], ["The Creative Builder", "Move from an open idea to a thoughtful, useful prototype.", "5 plots", Sprout], ["The Team Guide", "Create the shared habits that help teams do their best work.", "4 plots", Compass]] as const; return <PageShell><main><PageHero eyebrow="Guided learning paths" title="A little structure can make room for more growth." description="Choose a sequence that meets you where you are and carries you toward an outcome you can use." action={<Button asChild size="lg" className="rounded-full bg-primary text-primary-foreground"><a href="/courses">Explore all courses <ArrowRight /></a></Button>} /><section className="mx-auto max-w-6xl px-5 pb-20"><div className="grid gap-6 lg:grid-cols-3">{paths.map(([title, description, plots, Icon], index) => <article key={title} className={`vasant-rise rounded-[2rem] border border-white/70 p-7 shadow-lg shadow-blossom/10 ${index === 1 ? "bg-spring-wash" : index === 2 ? "bg-butter/30" : "bg-card"}`}><span className="grid size-12 place-items-center rounded-2xl bg-brand-gradient text-primary-foreground"><Icon /></span><p className="mt-8 text-xs font-semibold uppercase tracking-[0.18em] text-blossom-deep">{plots}</p><h2 className="mt-2 font-display text-3xl font-semibold text-ink">{title}</h2><p className="mt-3 leading-relaxed text-ink/70">{description}</p><Button variant="link" className="mt-6 px-0 text-blossom-deep">View path <ArrowRight /></Button></article>)}</div></section><section className="bg-sky/25"><div className="mx-auto grid max-w-6xl gap-10 px-5 py-16 lg:grid-cols-2"><div><SectionLabel>Why paths work</SectionLabel><h2 className="mt-3 font-display text-4xl font-semibold text-ink">Less tab-hopping. More momentum.</h2></div><div className="grid gap-5"><IconBullet icon={Leaf}>A beginning, middle, and end you can see before you start.</IconBullet><IconBullet icon={Sprout}>Small wins that make returning tomorrow feel natural.</IconBullet><IconBullet icon={Compass}>A final outcome that connects learning to real life.</IconBullet></div></div></section></main></PageShell>; }